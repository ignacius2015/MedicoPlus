<UserControl x:Class="MedicoPlus.Views.TopView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MedicoPlus.Views"
             xmlns:help="clr-namespace:MedicoPlus.Helpers"
             mc:Ignorable="d">
    
    <Grid>
        <ItemsControl ItemsSource="{Binding Documents}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Button Width="150" Height="150">
                            <Button.Background>
                                <ImageBrush ImageSource="{Binding Path=ImageName}"  TileMode="Tile" Stretch="UniformToFill"/>
                            </Button.Background>
                        </Button>
                        <TextBlock Grid.Row="1" TextWrapping="Wrap" TextAlignment="Center" Text="{Binding Path=LinkName}" FontSize="12"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>



